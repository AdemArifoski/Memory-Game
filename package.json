{
  "name": "server",
  "version": "1.0.0",
  "description": "A full stack browser game that uses a NodeJS backend, a SQLite database and JWT Authentication.",
  "main": "server.js",
  "scripts": {
    "dev":"node --watch --env-file=.env --experimental-strip-types --experimental-sqlite ./src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./src/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.2"
  }
}
